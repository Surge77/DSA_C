#include<stdio.h>

// initialization of a pointer

// Syntax:

// Pointer_name = &variable_name
// we will be initalizing the address of variable to a pointer

int main(){
    
    int a , *p = &a;

    // p = &a;

}